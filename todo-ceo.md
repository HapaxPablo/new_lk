# RMC / krasrm.com — сводка для CEO

Документ составлен по результатам аудита сайта, ТЗ программисту и анализа репозитория **new-lk** (Next.js 15, React 19, личный кабинет + публичный каталог на `krasrm.com` / `lk.krasrm.com`).

**Легенда статусов:** ✅ сделано · 🟡 частично · ❌ не сделано · 📋 вне кода (маркетинг/SEO-агентство)

---

## 1. Что уже есть в проекте

### Платформа и инфраструктура
- ✅ Next.js App Router, TypeScript, Docker, GitLab CI
- ✅ HTTPS, сессии (`iron-session`), авторизация (логин, регистрация, сброс пароля)
- ✅ Интеграция с API 1С (`API_1C_URL`), прокси и BFF-роуты в `app/api/*`
- ✅ Адаптивная вёрстка, определение mobile в `middleware.ts`
- ✅ Редирект **www → без www** (301) в `middleware.ts`
- ✅ Редирект `/` → `/nomenclatures`

### Публичные разделы (основной каталог)
| Раздел | Маршрут | Состояние |
|--------|---------|-----------|
| Каталог мест (номенклатуры) | `/nomenclatures`, `/nomenclatures/[id]` | ✅ список + карточка, SSR-загрузка данных на сервере |
| Бренды | `/brands`, `/brands/[slug]` | ✅ список + детальная |
| Арендаторы | `/tenants`, `/tenants/[id]` | ✅ |
| О компании | `/about` | ✅ |
| Аккредитация | `/accreditation` | ✅ |
| Оформление заказа | `/order` | ✅ |
| Акции | `/promotions`, `/promotions/[id]` | ✅ (в меню закомментированы) |
| Контрагенты | `/counterparties`, `/counterparties/[id]` | ✅ (в меню закомментированы) |

### Заглушки («в разработке»)
| Раздел | Маршрут |
|--------|---------|
| Ролики | `/content` |
| Медиапланы | `/media-plans` |
| Задачи | `/tasks` |
| Мои места | `/my-place` (есть metadata, UI — заглушка) |

### SEO и аналитика (уже в коде)
- ✅ `public/robots.txt`
- ✅ Динамический `app/sitemap.ts` — статические URL + **номенклатуры и бренды из API** (revalidate 1 ч)
- ✅ Canonical, Open Graph, keywords в `lib/configs/config-meta/`
- ✅ JSON-LD: Organization в `app/layout.tsx`, Product на карточках номенклатур
- ✅ Яндекс.Метрика (`YandexMetricaProvider`), e-commerce события
- ✅ Компонент хлебных крошек в UI + **JSON-LD BreadcrumbList** на части страниц (about, accreditation, brands/[slug], nomenclatures/[id], tenants/[id])
- ✅ Страница 404 (`app/not-found.tsx`)
- ✅ Google Site Verification — поле в metadata (нужен env)
- 🟡 Google Analytics — **не подключена** (в аудите: только Метрика)

### Прочее
- ✅ Mapbox, обратное геокодирование, формы обратной связи, модалки, пагинация, фильтры
- ✅ Внутренний `TODO.md` с фазами по контрагентам, акциям, контенту и т.д. (часть пунктов устарела — см. раздел 3)

---

## 2. Задачи из аудита и ТЗ программисту

Приоритет — то, что влияет на индексацию и трафик из поиска.

### 2.1. Критично для SEO (из аудита + ТЗ)

| # | Задача | Источник | Статус в проекте |
|---|--------|----------|------------------|
| 1 | **Полная карта сайта** — бренды, карточки товаров, позже города | ТЗ, аудит | 🟡 Исправлен сбор URL (`lib/sitemap/*`): пагинация, `api/nomenclatures/`, `api/brands/assigned`; после деплоя проверить `https://krasrm.com/sitemap.xml` (число URL >> 6). Города — когда появятся `/places/{город}` |
| 2 | **Кнопки → ссылки** на `/nomenclatures` и `/brands` («Подробнее» и переход в карточку) | ТЗ, аудит | ❌ Карточки на `Button` + `router.push` / `onClick` (`CardNomenclature`, `CardBrand`), робот не следует по ссылкам |
| 3 | **SSR / пререндеринг** — робот должен видеть список карточек на `/nomenclatures` | ТЗ, аудит | 🟡 Список номенклатур **fetch на сервере** в `page.tsx`, но оболочка списка — client-компоненты; нужна проверка «как видит Яндекс» после деплоя |
| 4 | **Шаблоны title, description, h1** по ТЗ (каталог, бренд, карточка, город) | ТЗ | ❌ Сейчас общие формулировки («Каталог номенклатуры», «Выберите места…»), не шаблоны из ТЗ |
| 5 | **h1 в карточке товара** — семантический заголовок с названием и адресом | ТЗ, аудит | ❌ На детальной странице название в `<span>`, отдельного `<h1>` нет |
| 6 | **Редиректы со старых URL** `/catalog/...` → новые `/nomenclatures/{uuid}` | ТЗ, аудит | ❌ В проекте нет правил; нужна таблица соответствий (старый slug → id) с бэкенда или выгрузки |
| 7 | **Посадочные страницы по городам** `/places/{город}` с фильтром площадок | ТЗ, аудит | ❌ Маршрутов `/places/*` нет; прототип: [Tailwind Play](https://play.tailwindcss.com/CdjWl261m9?layout=preview) |
| 8 | **Микроразметка хлебных крошек** на всех нужных страницах | ТЗ, аудит | 🟡 JSON-LD есть не везде; визуальные крошки не на всех разделах (например, списки `/nomenclatures`, `/brands`) |
| 9 | **Политика конфиденциальности** | ТЗ, аудит | ❌ |
| 10 | **Баннер согласия с cookies** | ТЗ, аудит | ❌ |
| 11 | **Редирект www → без www** | ТЗ, аудит | ✅ `middleware.ts` |

### 2.2. Шаблоны мета-тегов из ТЗ (нужно внедрить в код)

**`/nomenclatures`**
- Title: `Indoor реклама внутри помещений в Красноярске | «RMC»`
- H1: `Оператор Indoor рекламы - «RMC»`
- Description: по тексту ТЗ (тел. 8 800 222 59 38)

**`/brands/{slug}`**
- Title: `Размещение звуковой и видео рекламы внутри помещений {Бренд} | «RMC»`
- H1: `Indoor реклама в {Бренде}`
- Description: с `{Бренд}`, мин. ценой, телефоном

**`/nomenclatures/{id}`**
- Title: `Размещение Indoor рекламы в помещении {Название + адрес}`
- H1: `Размещение ролика на радио {Название + адрес}` *(уточнить формулировку с маркетингом — в ТЗ смешаны indoor и «радио»)*
- Description: по шаблону ТЗ

**`/places/{город}`** (новые страницы)
- Title / H1 / Description — по шаблону «Indoor реклама в {Городе}» из ТЗ

### 2.3. Задачи из аудита — в основном не разработка

| Задача | Кто |
|--------|-----|
| Уникальные тексты на сайте (сейчас «тексты отсутствуют») | Контент / маркетинг |
| Семантическое ядро, доработка внешних ссылок, disavow spam | SEO-агентство |
| PageSpeed 73/100 mobile — низкий приоритет, перепроверить после релиза | Dev + инфра |
| Подключить Google Analytics (если нужен) | Dev |
| Верификация в Google Search Console / Яндекс.Вебмастер (коды в env) | Dev + доступы CEO |
| ЧПУ вместо UUID в URL номенклатур — отдельное решение продукта | Продукт + бэкенд |

---

## 3. Внутренний бэклог разработки (из `TODO.md` + фактическое состояние)

| Фаза | План в TODO.md | Факт |
|------|----------------|------|
| Контрагенты | Создать с нуля | ✅ Уже реализовано |
| Акции | API + страницы | ✅ Реализовано (`app/api/promotions`, страницы) |
| Мои места | API + страница | 🟡 Страница есть, UI — заглушка |
| Ролики (content) | Полная реализация | ❌ Заглушка `PageDevelop` |
| Медиапланы | Полная реализация | ❌ Заглушка |
| Задачи | Полная реализация | ❌ Заглушка |
| Меню | Добавить разделы после API | 🟡 В `configMenuHeader.ts` в навигации только: номенклатуры, бренды, арендаторы, «О нас» |

**Технический долг (из кода):**
- Уведомления в шапке — не реализованы (комментарий в `Header.tsx`)
- Геолокация в шапке временно отключена
- Дублирование/несогласованность бренда в metadata («КрасРМ» / «RMC» / «ААР»)

---

## 4. Рекомендуемый порядок работ (для планирования)

### Спринт 1 — индексация (2–3 недели, dev)
1. Ссылки вместо кнопок на каталоге и брендах  
2. Проверка и починка sitemap на проде  
3. Шаблоны title / description / h1 по ТЗ  
4. h1 на карточке номенклатуры  
5. Breadcrumb JSON-LD на списках и недостающих страницах  

### Спринт 2 — миграция и compliance (1–2 недели)
6. Редиректы `/catalog/*` (нужны данные от 1С/старого сайта)  
7. Политика конфиденциальности + cookie banner  

### Спринт 3 — рост SEO (2–4 недели)
8. Посадочные `/places/{город}` + API фильтра по городу  
9. Добавление городов в sitemap  
10. Контентные тексты (совместно с маркетингом)  

### Параллельно (не dev)
- Аудит ссылочного профиля (60% spam в Ahrefs) — SEO  
- Настройка Search Console / Вебмастер — CEO + SEO  

---

## 5. Риски и зависимости

| Риск | Комментарий |
|------|-------------|
| Sitemap на проде пустой/короткий | Часто `API_1C_URL` недоступен при `next build` или на edge — без API в sitemap только статика (~6 URL) |
| Редиректы со старого каталога | Без маппинга старый ЧПУ → UUID задача блокируется |
| UUID в URL | Аудит отмечает «без ЧПУ»; для SEO лучше slug, но это изменение API/БД |
| Формулировки h1 «радио» vs «indoor» | Согласовать с маркетингом до вёрстки |

---

## 6. Источники

- `Аудит сайта RMC.docx` — технический и SEO-аудит (июнь 2026)  
- `ТЗ программисту RMC.docx` — задачи для разработки  
- Репозиторий: `C:\Users\pahap\work\new-lk`  
- Внутренний чеклист разработчиков: `TODO.md`  
- SEO-справка в проекте: `lib/configs/config-meta/SEO_GUIDE.md`  

---

*Дата сводки: 1 июня 2026*
