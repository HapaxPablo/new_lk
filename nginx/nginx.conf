server {
  listen 80;
  index index.php index.html index.htm index.nginx-debian.html;
  server_tokens off;

  # location / {
  #   alias /staticfiles/;
  #   try_files $uri $uri/ /index.html;
  # }

  # location /media/ {
  #   root /var/www/kittygram/;
  # }

  location / {
    proxy_pass http://localhost:3000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
  }

    #location /rmccopy/hs/MobileApp/ {
        # rewrite /api1c/(.*) /$1 break;
    #    proxy_set_header X-Real-IP $remote_addr;
     #   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #    proxy_set_header X-NginX-Proxy true;
    #    proxy_pass http://94.73.230.145:8080;
    #    # proxy_pass http://127.0.0.1:8080;
    #    # proxy_set_header Host $http_host;
    #    proxy_ssl_session_reuse off;
    #    proxy_redirect off;
    #}
    location /rmc/hs/MobileApp/ {
	
		# Добавляем заголовки CORS
		add_header 'Access-Control-Allow-Origin' '*' always;
		add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
		add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range' always;

		# Настройка для обработки предварительных (preflight) запросов
		if ($request_method = 'OPTIONS') {
        add_header 'Access-Control-Allow-Origin' '*';
        add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
        add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range';
        add_header 'Content-Type' 'text/plain; charset=utf-8';
        add_header 'Content-Length' 0;
        return 204;
		}

        # rewrite /api1c/(.*) /$1 break;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-NginX-Proxy true;
        proxy_pass http://192.168.0.2:8080;
        # proxy_pass http://127.0.0.1:8080;
        # proxy_set_header Host $http_host;
        proxy_ssl_session_reuse off;
        proxy_redirect off;
    }
	
	location /LoadContentWeb {
	
		# Добавляем заголовки CORS 
		# add_header 'Access-Control-Allow-Origin' '*' always;
		# add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
		# add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range' always;

		# # Настройка для обработки предварительных (preflight) запросов
		# if ($request_method = 'OPTIONS') {
        # add_header 'Access-Control-Allow-Origin' '*';
        # add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
        # add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Redirect';
        # add_header 'Content-Type' 'text/plain; charset=utf-8';
        # add_header 'Content-Length' 0;
        # return 204;
		# }

        # rewrite /api1c/(.*) /$1 break;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-NginX-Proxy true;
        proxy_pass http://192.168.0.5:8000;
        # proxy_pass http://127.0.0.1:8080;
        # proxy_set_header Host $http_host;
        proxy_ssl_session_reuse off;
        proxy_redirect off;
    }
    
	location /LoadNomenclaturePlacing {

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-NginX-Proxy true;
        proxy_pass http://192.168.0.5:8000;
        # proxy_pass http://127.0.0.1:8080;
        # proxy_set_header Host $http_host;
        proxy_ssl_session_reuse off;
        proxy_redirect off;
    }
	location /LoadNomenclature {

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-NginX-Proxy true;
        proxy_pass http://192.168.0.5:8000;
        # proxy_pass http://127.0.0.1:8080;
        # proxy_set_header Host $http_host;
        proxy_ssl_session_reuse off;
        proxy_redirect off;
    }
	location /GetContentFilters {

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-NginX-Proxy true;
        proxy_pass http://192.168.0.5:8000;
        # proxy_pass http://127.0.0.1:8080;
        # proxy_set_header Host $http_host;
        proxy_ssl_session_reuse off;
        proxy_redirect off;
    }
    location /fff {
        try_files $uri $uri/ =404;
    }

    location /firebase-cloud-messaging-push-scope {
        add_header Content-Type "text/javascript";
    }
}
