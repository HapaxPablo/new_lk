export type THttpMethod = 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE'

// Promotion types
export type {
  IPromotionList,
  IPromotionResponse,
  IPromotionDetails,
  IPromotionQueryParams,
} from './promotion'
