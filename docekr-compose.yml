version: '3'

services:
  newLk:
    build: .
    image: registry.krasrm.com/pahapasha11/new-lk
    restart: always

  nginx:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    ports:
      - '80:80'
    depends_on:
      - nextjs
