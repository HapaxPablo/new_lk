version: '3'

services:
  newLk:
    build: .
    image: registry.krasrm.com/pahapasha11/new-lk
    restart: always
    build: ./frontend
    ports:
      - 3000:3000

  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '80:80'
    depends_on:
      - newLk
